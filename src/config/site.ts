export const siteConfig = {
  name: "RNZ OS",
  tagline: "The AI Operating System for builders.",
  description:
    "Orchestrate projects, missions, AI agents and knowledge in one execution platform.",
  url: "https://rnz.os",
} as const;

export type SiteConfig = typeof siteConfig;
